import circlePattern from '/public/images/pattern-circle.svg';

const CirclePattern = () => {
  return (
    <img
      className='absolute top-0 left-0 z-0 -translate-x-[11.75rem] translate-y-[7.75rem]'
      src={circlePattern}
    />
  );
};

export default CirclePattern;
